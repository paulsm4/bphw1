import 'package:flutter/material.dart';

class SummaryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
      return Scaffold(
      appBar: AppBar(
        title: Text("BoxPik Trending Details"),
      ),
      body: Column(
        children: <Widget>[
          Text("Something"),
          Text("Something Else"),
        ],
      ),
    );
  }
}
